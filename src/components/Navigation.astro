---
import HeaderControls from "./HeaderControls";
import { products } from "../data/products";
import { extras } from "../data/extras";

const links = [
  { href: "/", label: "Home" },
  { href: "/store", label: "Store" },
  { href: "/extras", label: "Extras" },
  { href: "/#contact", label: "Contact" }
];

const announcement = "Free updates for life · 30-day refund";

const searchProducts = products.map((p) => ({
  slug: p.slug,
  name: p.name,
  description: p.description,
  tag: p.tag,
  category: p.category
}));

const searchExtras = extras.map((e) => ({
  slug: e.slug,
  name: e.name,
  description: e.description,
  tag: e.tag,
  category: e.category
}));
---

<header class="sticky top-0 z-40 border-b border-line bg-bg-soft/90 backdrop-blur-xl" data-navigation>
  <div class="border-b border-line/60">
    <div class="container mx-auto flex h-9 items-center justify-between px-4 text-[11px] font-semibold uppercase tracking-[0] text-ink-muted sm:px-6 lg:px-8">
      <p class="flex items-center gap-2">
        <span class="inline-block h-1.5 w-1.5 rounded-full bg-accent shadow-[0_0_10px_rgba(0,162,255,0.7)]"></span>
        <span>{announcement}</span>
      </p>
      <div class="hidden items-center gap-4 sm:flex">
        <a href="/store" class="transition hover:text-ink">Browse store</a>
        <span aria-hidden="true" class="opacity-30">·</span>
        <a href="mailto:hello@krt.example" class="transition hover:text-ink">hello@krt.example</a>
      </div>
      <a href="/store" class="sm:hidden transition hover:text-ink">Browse store</a>
    </div>
  </div>

  <div class="container mx-auto flex h-16 items-center justify-between gap-4 px-4 sm:px-6 lg:px-8">
    <a href="/" class="group flex items-center gap-3" aria-label="KRT store home">
      <span class="grid h-9 w-9 place-items-center rounded-xl bg-accent font-black text-ink-inverse shadow-[0_0_18px_rgba(0,162,255,0.45)] transition group-hover:scale-105">
        K
      </span>
      <span class="text-base font-black tracking-[-0.04em] text-ink">KRT store</span>
    </a>

    <nav class="hidden items-center gap-1 md:flex" aria-label="Primary navigation">
      {links.map((link) => (
        <a
          href={link.href}
          class="rounded-full px-4 py-2 text-sm font-semibold text-ink-muted transition hover:bg-slate-800/40 hover:text-ink"
        >
          {link.label}
        </a>
      ))}
    </nav>

    <div class="flex items-center gap-2">
      <HeaderControls client:load products={searchProducts} extras={searchExtras} />
    </div>
  </div>

  <div class="container mx-auto hidden border-t border-line/60 md:block" data-mobile-target>
    <div class="flex items-center gap-1 overflow-x-auto px-4 py-3 text-sm sm:px-6 lg:px-8">
      <span class="mr-2 text-[11px] font-black uppercase tracking-[0.2em] text-ink-muted">
        Sections
      </span>
      <a href="/" class="whitespace-nowrap rounded-full border border-line bg-surface/60 px-3 py-1.5 text-xs font-bold text-ink-muted transition hover:border-accent/40 hover:text-ink">Home</a>
      <a href="/store" class="whitespace-nowrap rounded-full border border-line bg-surface/60 px-3 py-1.5 text-xs font-bold text-ink-muted transition hover:border-accent/40 hover:text-ink">Store</a>
      <a href="/extras" class="whitespace-nowrap rounded-full border border-line bg-surface/60 px-3 py-1.5 text-xs font-bold text-ink-muted transition hover:border-accent/40 hover:text-ink">Extras</a>
      <a href="/#contact" class="whitespace-nowrap rounded-full border border-line bg-surface/60 px-3 py-1.5 text-xs font-bold text-ink-muted transition hover:border-accent/40 hover:text-ink">Contact</a>
    </div>
  </div>

  <div class="hidden border-t border-line bg-bg-soft md:hidden" data-mobile-panel>
    <nav class="container mx-auto flex flex-col gap-1 px-4 py-4 sm:px-6" aria-label="Mobile primary">
      {links.map((link) => (
        <a
          href={link.href}
          class="rounded-2xl px-4 py-3 text-sm font-bold text-ink-muted transition hover:bg-slate-800/40 hover:text-ink"
        >
          {link.label}
        </a>
      ))}
      <a href="/cart" class="rounded-2xl px-4 py-3 text-sm font-bold text-ink-muted transition hover:bg-slate-800/40 hover:text-ink">
        Cart
      </a>
      <a href="/store" class="mt-2 rounded-2xl bg-accent px-4 py-3 text-center text-sm font-black text-white shadow-[0_0_22px_rgba(0,162,255,0.35)] transition hover:bg-accent-bright">
        Browse store
      </a>
    </nav>
    <div class="container mx-auto border-t border-line/60 px-4 pb-4 pt-3 sm:px-6">
      <p class="mb-2 text-[11px] font-black uppercase tracking-[0.2em] text-ink-muted">Sections</p>
      <div class="flex flex-wrap gap-2">
        <a href="/" class="rounded-full border border-line bg-surface/60 px-3 py-1.5 text-xs font-bold text-ink-muted">Home</a>
        <a href="/store" class="rounded-full border border-line bg-surface/60 px-3 py-1.5 text-xs font-bold text-ink-muted">Store</a>
        <a href="/extras" class="rounded-full border border-line bg-surface/60 px-3 py-1.5 text-xs font-bold text-ink-muted">Extras</a>
        <a href="/#contact" class="rounded-full border border-line bg-surface/60 px-3 py-1.5 text-xs font-bold text-ink-muted">Contact</a>
      </div>
    </div>
  </div>
</header>

<script is:inline>
  (function () {
    const panel = document.querySelector("[data-mobile-panel]");
    const trigger = document.querySelector("[data-mobile-trigger]");
    if (!panel) return;
    const setOpen = (open) => {
      panel.classList.toggle("hidden", !open);
      if (trigger) trigger.setAttribute("aria-expanded", String(open));
    };
    window.addEventListener("krt:toggle-menu", () => {
      const isOpen = !panel.classList.contains("hidden");
      setOpen(!isOpen);
    });
    panel.querySelectorAll("a").forEach((anchor) =>
      anchor.addEventListener("click", () => {
        panel.classList.add("hidden");
        if (trigger) trigger.setAttribute("aria-expanded", "false");
      })
    );

    document.addEventListener("keydown", (event) => {
      if (event.key === "Escape" && !panel.classList.contains("hidden")) {
        setOpen(false);
      }
    });
  })();
</script>
